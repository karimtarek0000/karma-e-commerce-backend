import { categoryModel } from "../../../DB/models/Category.model.js";

export const createNewCategory = (req, res, next) => {
  // 1 - make sure name of category is unique
  // 2 - make sure upload the image
  // 3 - after upload image update the db
};
